import Settings from "../Settings/Settings";

export default function NormalSettings() {
  return (
    <section aria-label='normal setting' className='hidden md:flex w-1/5 rounded-l-md flex-col justify-between'>
      <Settings />
    </section>
  );
}
